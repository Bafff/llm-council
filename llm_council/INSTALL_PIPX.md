# 📦 Установка через pipx

## Что такое pipx?

pipx - это инструмент для установки Python CLI приложений в изолированные окружения. Это как `npm install -g`, но для Python.

**Преимущества:**
- Изолированное окружение (не конфликтует с другими пакетами)
- Глобальная команда `llm-council` доступна везде
- Легкое обновление одной командой

---

## 1. Установка pipx (если еще нет)

```bash
# macOS / Linux
python3 -m pip install --user pipx
python3 -m pipx ensurepath

# После установки перезагрузите терминал или:
source ~/.bashrc  # или ~/.zshrc
```

---

## 2. Установка LLM Council

### Из локальной директории (для разработки):

```bash
cd /home/user/claude-code-council
pipx install -e ./llm_council
```

**Флаг `-e` (editable)**: изменения в коде сразу доступны без переустановки

### Из Git репозитория (для пользователей):

```bash
pipx install git+https://github.com/Bafff/claude-code-council.git#subdirectory=llm_council
```

---

## 3. Использование

После установки команда `llm-council` доступна глобально:

```bash
# Спросить вопрос
llm-council ask "What is async/await?"

# Короткий синтаксис
llm-council "What is Docker?"

# Посмотреть модели
llm-council models

# Настроить
llm-council setup

# Версия
llm-council version
```

**Также доступен алиас `council`:**

```bash
council ask "What is Python?"
council models
```

---

## 4. Обновление

### После изменений в коде:

```bash
pipx reinstall llm_council
```

### Или из Git:

```bash
pipx upgrade llm_council
```

---

## 5. Удаление

```bash
pipx uninstall llm_council
```

---

## 🚀 Одна команда для всего

После установки через pipx:

```bash
llm-council ask "Explain quantum computing"
```

**Работает из любой директории!** ✨

---

## Решение проблем

### "llm-council: command not found"

```bash
# Проверьте путь
pipx ensurepath

# Перезагрузите терминал
source ~/.bashrc

# Или добавьте в PATH вручную
export PATH="$HOME/.local/bin:$PATH"
```

### "ModuleNotFoundError"

```bash
# Переустановите
pipx uninstall llm_council
pipx install -e /path/to/llm_council
```

### Обновить после изменений

```bash
pipx reinstall llm_council
```

---

## Сравнение: pip vs pipx

| Метод | Команда установки | Где работает | Изоляция |
|-------|------------------|--------------|----------|
| **pip** | `pip install .` | Нужно быть в директории | ❌ Глобальное |
| **pipx** | `pipx install -e .` | Везде! | ✅ Изолированное |

**Рекомендация:** Используйте pipx для CLI приложений, pip для библиотек.
